name: code-scheduler

on:
  schedule:
    - cron: "0 15 * * *"
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: post-code
        run: |
          curl -X POST ${{secrets.API_URL}}
